import { LocalImageUrlPipe } from './local-image-url.pipe';

describe('LocalImageUrlPipe', () => {
  it('create an instance', () => {
    // const pipe = new LocalImageUrlPipe();
    // expect(pipe).toBeTruthy();
  });
});
